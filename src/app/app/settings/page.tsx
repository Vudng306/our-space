import SettingsView from "./SettingsView";

export const dynamic = "force-dynamic";
export const metadata = { title: "Settings · Our Space" };

export default function SettingsPage() {
  return <SettingsView />;
}
